#include <vector>
#include <string>
#include "InventoryItem.h"
#include <QList>
//Create objects for system

#ifndef INVENTORYMANAGER_H
#define INVENTORYMANAGER_H

#include <QList>
/*
class InventoryManager {
public:
	void addItem(const InventoryItem& itemType);
	void editItem(int index, const InventoryItem& itemType);
	void deleteItem(int index);
	QList<InventoryItem> getItems() const;
	QList <InventoryItem> searchItems(const QString& keyword) const;
	void loadFromFile(const QString& filepath);
	void saveToFile(const QString& filepath) const;

private:
	QList<InventoryItem> items;
};

#endif //INVENTORYMANAGER_H
*/